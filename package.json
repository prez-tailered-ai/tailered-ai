{
  "name": "@tailered-ai/core",
  "version": "0.1.0",
  "description": "Mint and operate AI-native companies as plain files in a Git repository.",
  "type": "module",
  "bin": {
    "tailered": "./dist/src/cli.js"
  },
  "files": [
    "dist",
    "benchmarks",
    "docs",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js",
    "demo": "npm run build && node dist/src/cli.js demo",
    "validate": "npm run build && node dist/src/cli.js validate --repo ."
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  },
  "license": "Apache-2.0"
}
