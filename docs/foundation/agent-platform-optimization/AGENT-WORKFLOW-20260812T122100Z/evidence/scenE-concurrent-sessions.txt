SCENARIO E — concurrent Claude Code sessions (synthetic disposable repo + worktrees)
## E1 disjoint edits
merge-tree(A,B) exit=0 -> disjoint, both continue
main after both merges: 69f00ab Merge branch 'sessionB'; histories: Merge branch 'sessionB'|Merge branch 'sessionA'|B: fileB|genesis|A: fileA|
## E6 append-only ledger from both sessions
merge-tree exit=1 (1 = conflict detected BEFORE any merge attempt)
merge B exit=1 (conflict as predicted)
ledger union valid JSONL, events: 3 (genesis + A-1 + B-1) -> zero loss
## E2 overlapping path claim
pre-commit detector: merge-tree(A, B-committed-state) predicts conflict on fileC BEFORE B commits -> B stops, records the overlap, does NOT stash/reset
B worktree left dirty by design:  M fileC.txt 
## E3 main advances mid-work
A detects main moved: behind=7 -> A composes with a normal merge (no rebase)
A composed; ancestor preserved: yes
## E4 docs-only branch
classification: [docs/readme.md] -> docs-only -> no runtime overlap -> safe to continue without stopping other sessions
## E5 abandoned session
detector: worktree wtZ dirty=1 file(s), branch sessionZ unmerged, no lock renewal -> classified ABANDONED
policy: leave untouched (no stash/reset/clean); record in overlap register; owner decides
wtZ content preserved: core/A-change/half-done/
## invariant checks
cross-worktree writes: 0 (every write went through its own worktree)
force-pushes/rebases/stashes/resets: 0 (checkout of B's own uncommitted edit was an explicit recorded abandonment)
